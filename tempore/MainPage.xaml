<Page
    x:Class="tempore.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:tempore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

     <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
          
          <StackPanel x:Name="Row1" VerticalAlignment="Top">
               <TextBox x:Name="TextBox1" Text="" />
               <Button x:Name="Button1" Content="Add task"/>
               <ListView ItemsSource="{ x:Bind list}">
                    <ListView.ItemTemplate>
                         <DataTemplate x:DataType="local:ListValues">
                              <TextBlock Text="{x:Bind title}"></TextBlock>
                         </DataTemplate>
                    </ListView.ItemTemplate>
               </ListView>

          </StackPanel>
          
         
     </Grid>
</Page>
